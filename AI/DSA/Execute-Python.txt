A full set of 100 questions with complete answers, logic, examples, and Python code would be the size of a small book (200+ pages). For interview preparation, it's much more effective to organize them by pattern and master the underlying logic.

For someone with your background (18+ years experience, struggling mostly with coding-round speed and pattern recognition), I recommend learning the Top 100 LeetCode questions grouped by pattern. Once you recognize the pattern, the implementation becomes much easier.

Top 100 Coding Questions by Pattern
1. HashMap Pattern (10 Questions)
Core Logic

Use a dictionary when you need:

Fast lookup O(1)
Frequency counting
Remembering previously seen values
Questions
Two Sum
Contains Duplicate
Valid Anagram
Intersection of Two Arrays
Happy Number
Longest Consecutive Sequence
Top K Frequent Elements
Group Anagrams
Isomorphic Strings
Subarray Sum Equals K
Example 1: Two Sum
Problem
nums = [2,7,11,15]
target = 9

Answer = [0,1]
Logic
Need two numbers whose sum = target

Current Number = 7

Target - Current = 2

Have we seen 2 before?

YES

Return indices
Python
def two_sum(nums,target):

    seen = {}

    for i,num in enumerate(nums):

        diff = target - num

        if diff in seen:
            return [seen[diff],i]

        seen[num] = i
Time Complexity
O(n)
2. Two Pointer Pattern (10 Questions)
When To Use
Sorted Array
Palindrome
Pair Finding
Questions
Valid Palindrome
Two Sum II
Container With Most Water
3 Sum
Remove Duplicates Sorted Array
Move Zeroes
Squares of Sorted Array
Merge Sorted Array
Trapping Rain Water
Reverse String
Example
Valid Palindrome
def isPalindrome(s):

    left = 0
    right = len(s)-1

    while left < right:

        if s[left] != s[right]:
            return False

        left += 1
        right -= 1

    return True
3. Sliding Window (10 Questions)
Keywords
Longest
Shortest
Substring
Subarray
Continuous
Questions
Longest Substring Without Repeating Characters
Maximum Average Subarray
Minimum Size Subarray Sum
Permutation in String
Find All Anagrams
Maximum Vowels
Fruits Into Baskets
Max Consecutive Ones
Longest Repeating Character Replacement
Sliding Window Maximum
Example
Longest Substring Without Repeating Characters
def lengthOfLongestSubstring(s):

    left = 0
    seen = set()
    result = 0

    for right in range(len(s)):

        while s[right] in seen:
            seen.remove(s[left])
            left += 1

        seen.add(s[right])

        result = max(result,right-left+1)

    return result
4. Stack Pattern (10 Questions)
Questions
Valid Parentheses
Min Stack
Daily Temperatures
Next Greater Element
Evaluate Reverse Polish Notation
Remove Adjacent Duplicates
Simplify Path
Basic Calculator
Decode String
Largest Rectangle Histogram
Example
Valid Parentheses
def isValid(s):

    stack = []

    mapping = {
        ')':'(',
        ']':'[',
        '}':'{'
    }

    for ch in s:

        if ch in mapping:

            if not stack:
                return False

            top = stack.pop()

            if top != mapping[ch]:
                return False

        else:
            stack.append(ch)

    return len(stack) == 0
5. Linked List (10 Questions)
Questions
Reverse Linked List
Merge Two Sorted Lists
Linked List Cycle
Remove Nth Node
Middle of Linked List
Palindrome Linked List
Reorder List
Add Two Numbers
Copy List Random Pointer
Intersection of Linked Lists
Example
Reverse Linked List
def reverseList(head):

    prev = None
    curr = head

    while curr:

        nxt = curr.next

        curr.next = prev

        prev = curr

        curr = nxt

    return prev
6. Binary Search (10 Questions)
Questions
Binary Search
Search Insert Position
First Bad Version
Search Rotated Array
Find Peak Element
Koko Eating Bananas
Capacity To Ship Packages
Split Array Largest Sum
Median of Two Sorted Arrays
Search 2D Matrix
Binary Search Template
def binary_search(nums,target):

    left = 0
    right = len(nums)-1

    while left <= right:

        mid = (left+right)//2

        if nums[mid] == target:
            return mid

        elif nums[mid] < target:
            left = mid+1

        else:
            right = mid-1

    return -1
7. Tree DFS (10 Questions)
Questions
Maximum Depth
Same Tree
Symmetric Tree
Path Sum
Diameter of Binary Tree
Balanced Binary Tree
Invert Binary Tree
Subtree of Another Tree
Validate BST
Lowest Common Ancestor
DFS Template
def dfs(root):

    if not root:
        return

    dfs(root.left)

    dfs(root.right)
8. Tree BFS (10 Questions)
Questions
Binary Tree Level Order
Zigzag Traversal
Right Side View
Minimum Depth
Average Levels
Cousins in Tree
Bottom Left Value
Connect Next Right Pointer
Maximum Level Sum
Vertical Order Traversal
BFS Template
from collections import deque

def bfs(root):

    q = deque([root])

    while q:

        node = q.popleft()

        if node.left:
            q.append(node.left)

        if node.right:
            q.append(node.right)
9. Graph DFS/BFS (10 Questions)
Questions
Number of Islands
Clone Graph
Course Schedule
Pacific Atlantic Water Flow
Word Search
Surrounded Regions
Graph Valid Tree
Number Connected Components
Redundant Connection
Open Lock
Number Of Islands
Logic
Found land

Explore all connected land

Count = 1 island
DFS
def dfs(r,c):

    if r<0 or c<0 or r>=rows or c>=cols:
        return

    if grid[r][c] == "0":
        return

    grid[r][c] = "0"

    dfs(r+1,c)
    dfs(r-1,c)
    dfs(r,c+1)
    dfs(r,c-1)
10. Heap / Priority Queue (10 Questions)
Questions
Kth Largest Element
Top K Frequent
Merge K Sorted Lists
Task Scheduler
Meeting Rooms II
Find Median Data Stream
IPO
Last Stone Weight
Reorganize String
K Closest Points
Kth Largest
import heapq

def kthLargest(nums,k):

    heap = []

    for n in nums:

        heapq.heappush(heap,n)

        if len(heap) > k:
            heapq.heappop(heap)

    return heap[0]
11. Dynamic Programming (10 Questions)
Questions
Climbing Stairs
House Robber
Coin Change
Longest Increasing Subsequence
Maximum Product Subarray
Partition Equal Subset Sum
Decode Ways
Word Break
Unique Paths
Longest Common Subsequence
Climbing Stairs
Logic
Step 5

Can come from:

Step 4
or
Step 3

dp[i] = dp[i-1] + dp[i-2]
Python
def climbStairs(n):

    if n <= 2:
        return n

    a,b = 1,2

    for _ in range(3,n+1):
        a,b = b,a+b

    return b
12. Backtracking (10 Questions)
Questions
Permutations
Combination Sum
Subsets
N Queens
Letter Combinations Phone Number
Generate Parentheses
Sudoku Solver
Palindrome Partitioning
Restore IP Address
Word Search
Generate Parentheses
def backtrack(openCount,closeCount):

    if openCount == n and closeCount == n:
        return

    if openCount < n:
        backtrack(openCount+1,closeCount)

    if closeCount < openCount:
        backtrack(openCount,closeCount+1)
Coding Test Strategy

If you have only 2-3 weeks:

Week 1
HashMap (10)
Two Pointers (10)
Sliding Window (10)
Week 2
Stack (10)
Linked List (10)
Binary Search (10)
Week 3
Trees DFS/BFS (20)
Graphs (10)
Heap (10)
Final
Dynamic Programming (10)

This set of 100 questions covers roughly 90%+ of Amazon, Microsoft, Walmart, PayPal, Dell, Oracle, Capital One, and many hackathon-style coding tests. For your situation, I would start with the first 30 questions (HashMap + Two Pointers + Sliding Window) because those patterns appear most frequently in online assessments.